import {
  Button,
  Modal,
  ModalBody,
  ModalCloseButton,
  ModalContent,
  ModalFooter,
  ModalHeader,
  Text,
} from "@chakra-ui/react";
import styled from "@emotion/styled";

export const CompeModalContainer = styled(Modal)``;
export const CompeModalContent = styled(ModalContent)``;
export const CompeModalHeader = styled(ModalHeader)``;
export const CompeModalCloseButton = styled(ModalCloseButton)``;
export const CompeModalBody = styled(ModalBody)``;
export const CompeModalText = styled(Text)``;
export const CompeModalFooter = styled(ModalFooter)``;
export const CompeModalButton = styled(Button)``;
